name: bootstrap retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap is retired; use the permanent workflows in this repository."
